# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
.vite/

# Env & secrets
.env
*.local

# Локальный override docker-compose (например, другой порт Postgres, если
# 5432 занят чем-то ещё на конкретной машине) - машинно-специфичный, не коммитим.
docker-compose.override.yml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# CV model (downloaded at runtime / by calibrate script)
*.task

# Локальные тестовые видео + офлайн-калибровка (не коммитим)
test_videos/
